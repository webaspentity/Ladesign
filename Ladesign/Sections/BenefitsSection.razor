<section class="benefits-section">
    <Container>
        <SectionTitle>Наша команда дает уверенность</SectionTitle>
        <div class="benefits-section__body">
            <div class="benefits-section__content">
                <p class="benefits-section__text">
                    Дизайн-проект от студии LA Design не только совершенен по дизайну, но и реализуем
                </p>
                <div class="benefits-section__spoilers">
                    <CascadingValue Value="current">
                        @for (int i = 0; i < AppData.SpoilersData.Count; i++)
                        {
                            <Spoiler Item="AppData.SpoilersData[i]" Order="@i" OnClick="SetCurrent"/>
                        }
                    </CascadingValue>
                </div>
            </div>
            <div class="benefits-section__image">
                <img src="images/benefits_image.jpg" alt="Дизайн-проект" />
            </div>
        </div>
    </Container>
</section>

@code {
    public int current = -1;

    private void SetCurrent(int order)
    {
        if (current == order) current = -1; 
        else current = order;
    }
}
